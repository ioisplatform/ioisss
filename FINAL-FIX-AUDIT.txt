IOIS FINAL V2 — LOCAL CODE AUDIT
=================================

Passed:
- JavaScript syntax check: all .js files passed Node --check.
- Local HTML href/src references: no missing local file references after asset normalization.
- Canonical registration page: register.html.
- Legacy registration.html redirects to register.html.
- Duplicate registration controller disabled.
- Storage bucket reference standardized to member-documents in the active registration controller.
- Storage paths use <auth.uid()>/<file>, matching the owner-only Storage policies.
- Member ID generation moved to Supabase RPC with plan-wise row locking.
- RPC is idempotent for the same Auth user.
- Referral URL is based on the permanent Member ID.
- Public frontend does not contain a service_role key or Telegram bot token.

Supabase setup still required:
- Run IOIS-SUPABASE-PRODUCTION-SETUP.sql once.
- Keep Auth > Confirm sign up enabled.
- Configure production SMTP if more than the default testing email allowance is needed.
- Create the intended admin_profiles row for the actual admin account.

Cannot be verified from a ZIP alone:
- Live Supabase RLS behavior in your actual project
- Email provider delivery
- Existing database rows and data quality
- Payment gateway/UPI settlement
- Telegram Edge Function deployment
